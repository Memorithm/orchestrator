name: materialize strict autonomous merge policy

on:
  push:
    branches: [research/safe-automerge]
    paths:
      - tools/apply-safe-automerge.py
      - tools/finalize-safe-automerge.py
      - .github/workflows/materialize-safe-automerge.yml

permissions:
  contents: write

concurrency:
  group: orchestrator-safe-automerge-materialization
  cancel-in-progress: false

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply strict autonomous merge policy
        run: |
          python3 tools/apply-safe-automerge.py
          python3 tools/finalize-safe-automerge.py

      - name: Remove one-shot materializer
        run: |
          rm tools/apply-safe-automerge.py
          rm tools/finalize-safe-automerge.py
          rm .github/workflows/materialize-safe-automerge.yml

      - name: Rust toolchain
        run: |
          rustup update stable
          rustup default stable
          rustup component add rustfmt clippy

      - name: Format integrated sources
        run: cargo fmt --all

      - name: Validate runtime shell
        run: |
          bash -n scripts/start.sh
          bash -n scripts/opencode
          bash -n scripts/opencode-env
          bash -n scripts/cargo
          bash -n scripts/git
          bash -n scripts/gh
          bash -n scripts/agent-git
          bash -n scripts/agent-gh
          bash -n scripts/selftest-runtime.sh
          bash -n scripts/selftest-opencode-watchdog.sh
          bash -n scripts/render-systemd-unit.sh
          bash -n scripts/install-systemd.sh
          bash scripts/selftest-runtime.sh
          bash scripts/selftest-opencode-watchdog.sh

      - name: Verify systemd sandbox
        run: |
          mkdir -p "$RUNNER_TEMP/orchestrator-data"
          touch "$RUNNER_TEMP/orchestrator.env"
          bash scripts/render-systemd-unit.sh \
            "$GITHUB_WORKSPACE" \
            "$RUNNER_TEMP/orchestrator-data" \
            "$RUNNER_TEMP/orchestrator.env" \
            > "$RUNNER_TEMP/memorithm-orchestrator.service"
          systemd-analyze verify "$RUNNER_TEMP/memorithm-orchestrator.service"

      - name: Validate strict merge runtime
        run: |
          cargo check --workspace
          cargo test --workspace
          cargo clippy --workspace --all-targets -- -D warnings
          git diff --check

      - name: Enforce materialized scope
        run: |
          changed="$(git status --short | awk '{print $2}')"
          printf '%s\n' "$changed"
          for expected in \
            src/main.rs \
            src/merge_policy.rs \
            scripts/install-systemd.sh \
            tools/apply-safe-automerge.py \
            tools/finalize-safe-automerge.py \
            .github/workflows/materialize-safe-automerge.yml
          do
            printf '%s\n' "$changed" | grep -Fxq "$expected"
          done
          unexpected="$(printf '%s\n' "$changed" | grep -Ev '^(src/main\.rs|src/merge_policy\.rs|scripts/install-systemd\.sh|tools/apply-safe-automerge\.py|tools/finalize-safe-automerge\.py|\.github/workflows/materialize-safe-automerge\.yml)$' || true)"
          test -z "$unexpected"

      - name: Commit materialized merge policy
        run: |
          git config user.name 'Memorithm Orchestrator Integration'
          git config user.email 'orchestrator@localhost'
          git add -A
          git commit -m 'feat(merge): require exact locally validated PR heads'
          git push origin HEAD:research/safe-automerge
